# Opsight 验收测试报告

> 版本：v0.2.0 | 测试日期：________  
> 测试人员：________ | 审批人：________

---

## 1. 测试环境信息

| 项目 | 详情 |
|------|------|
| 服务器 IP | |
| 操作系统 | |
| CPU | |
| 内存 | |
| 磁盘 | |
| Docker 版本 | |
| Opsight 版本 | |
| 浏览器 | |
| 测试网络 | |
| 数据库版本 | |

---

## 2. 功能测试

### 2.1 登录模块

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| AUTH-001 | 正常登录 | 输入正确邮箱和密码，点击登录 | 跳转到 Dashboard，Token 保存到浏览器 | | |
| AUTH-002 | 错误密码登录 | 输入正确邮箱和错误密码 | 显示"邮箱或密码错误"提示 | | |
| AUTH-003 | 空字段提交 | 不输入邮箱和密码直接登录 | 显示必填字段提示 | | |
| AUTH-004 | 登出 | 登录后点击退出按钮 | 清除 Token，返回登录页面 | | |
| AUTH-005 | 未登录访问 | 清除 Token 后访问任意页面 | 自动跳转到登录页面 | | |
| AUTH-006 | Token 过期 | 使用过期 Token 访问 API | 返回 401，跳转到登录页 | | |
| AUTH-007 | 连续错误锁定 | 连续 5 次输入错误密码 | 账户被临时锁定 15 分钟 | | |

### 2.2 Dashboard 仪表盘

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| DASH-001 | KPI 卡片展示 | 登录后进入 Dashboard 页面 | 显示活跃事件数、MTTR、健康服务数、AI 告警数等卡片 | | |
| DASH-002 | 错误率趋势图 | 查看 Error Rate 区域 | 显示 24 小时折线图，数据点正确 | | |
| DASH-003 | 延迟对比图 | 查看 Latency 区域 | 显示各服务 P50/P90/P99 柱状图 | | |
| DASH-004 | Top 错误列表 | 查看 Top Errors 区域 | 显示 Top 5 错误，含次数和趋势标识 | | |
| DASH-005 | 服务状态网格 | 查看 Service Health 区域 | 显示所有服务卡片，颜色对应健康状态 | | |
| DASH-006 | 数据自动刷新 | 保持 Dashboard 页面打开 | WebSocket 推送实时更新数据 | | |

### 2.3 Incidents 故障管理

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| INC-001 | 故障列表展示 | 点击 Incidents 菜单 | 显示所有故障事件列表，含 ID、摘要、状态、时间 | | |
| INC-002 | 状态筛选 | 点击状态筛选下拉框，选择 "critical" | 仅显示 critical 状态的事件 | | |
| INC-003 | 服务筛选 | 选择特定服务筛选 | 仅显示该服务的故障事件 | | |
| INC-004 | 关键词搜索 | 搜索框中输入关键词 | 筛选摘要中包含关键词的事件 | | |
| INC-005 | 展开故障详情 | 点击故障行 | 展开显示详情，含 detail 字段 | | |
| INC-006 | 解决故障 | 点击 Resolve 按钮 | 故障状态变为 resolved | | |
| INC-007 | 重置筛选 | 点击重置按钮 | 清除所有筛选条件，显示全部事件 | | |

### 2.4 Services 服务管理

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SVC-001 | 服务列表展示 | 点击 Services 菜单 | 显示所有服务，含名称、状态、RPS、延迟、错误率 | | |
| SVC-002 | 服务详情 | 点击某个服务行 | 展开显示详细指标和依赖信息 | | |
| SVC-003 | 依赖矩阵展示 | 查看服务依赖关系 | 显示该服务依赖的其他服务列表 | | |
| SVC-004 | 状态颜色区分 | 查看不同状态的服务 | healthy=绿色、degraded=黄色、down=红色 | | |
| SVC-005 | 服务指标格式化 | 查看各服务的指标显示 | RPS 带千分位、延迟带 ms、错误率带 % | | |

### 2.5 Alert Rules 告警规则

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| ALR-001 | 规则列表展示 | 点击 Alert Rules 菜单 | 显示所有告警规则，含名称、条件、阈值、严重级别 | | |
| ALR-002 | 启用规则 | 点击已禁用规则的开关 | 规则状态变为启用 | | |
| ALR-003 | 禁用规则 | 点击已启用规则的开关 | 规则状态变为禁用 | | |
| ALR-004 | AI 标记识别 | 查看规则列表 | AI 生成的规则有特定标识 | | |
| ALR-005 | 严重级别筛选 | 按严重级别查看 | 按 critical/warning/info 筛选正确 | | |

### 2.6 Metrics 指标查询

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| MET-001 | 指标选择器 | 点击指标下拉框 | 显示可用指标列表（cpu_usage, memory_usage 等） | | |
| MET-002 | 图表展示 | 选择指标后查看图表 | 显示时序折线图，含 min/avg/p95/p99 | | |
| MET-003 | 服务筛选 | 选择特定服务进行筛选 | 图表数据限定为该服务 | | |
| MET-004 | 数据表格 | 查看表格视图 | 显示原始时序数据 | | |
| MET-005 | 切换指标 | 切换不同指标 | 图表立即更新为新指标数据 | | |

### 2.7 Topology 服务拓扑

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| TOP-001 | 拓扑节点展示 | 点击 Topology 菜单 | 显示所有服务节点，含名称和状态 | | |
| TOP-002 | 连接线展示 | 查看节点间的连线 | 有依赖关系的服务之间显示连线 | | |
| TOP-003 | 节点状态显示 | 查看各节点 | 节点颜色对应健康状态 | | |
| TOP-004 | 点击节点详情 | 点击某个服务节点 | 显示该服务详细信息和 RCA 入口 | | |
| TOP-005 | RCA 分析 | 对有问题的服务执行 RCA | 显示故障链路、根因和置信度 | | |

### 2.8 AI Insights AI 洞察

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| INS-001 | 标签页切换 | 点击不同 Tab（Root Cause/Predictions 等） | 切换到对应分类的洞察卡片 | | |
| INS-002 | 洞察卡片展示 | 查看洞察列表 | 每张卡片含标题、正文、服务、置信度、时间 | | |
| INS-003 | 严重级别标识 | 查看卡片 | 卡片颜色/图标对应严重级别 | | |
| INS-004 | 关联事件跳转 | 点击卡片中关联的事件 ID | 跳转到对应事件详情 | | |

### 2.9 Integrations 集成管理

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| ITG-001 | 集成卡片展示 | 点击 Integrations 菜单 | 显示所有集成，每个集成一张卡片 | | |
| ITG-002 | 卡片信息展示 | 查看每张集成卡片 | 含名称、类型、状态、事件数量 | | |
| ITG-003 | 状态标识 | 查看不同状态的集成 | connected/disconnected 有不同样式 | | |

### 2.10 Team 团队管理

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| TEA-001 | 成员表格展示 | 点击 Team 菜单 | 显示团队成员表格，含姓名、邮箱、角色、团队 | | |
| TEA-002 | 角色展示 | 查看成员角色列 | 显示 Admin/Editor/Viewer 等角色 | | |
| TEA-003 | 团队分组 | 查看团队列 | 成员按团队分组显示 | | |

### 2.11 Audit 审计日志

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| AUD-001 | 审计日志列表 | 进入审计页面 | 显示操作记录列表，含操作人、操作类型、时间 | | |
| AUD-002 | 时间筛选 | 选择时间范围 | 仅显示该时间段的记录 | | |
| AUD-003 | 操作类型筛选 | 按操作类型筛选 | 仅显示指定类型的操作 | | |
| AUD-004 | 导出功能 | 点击导出按钮 | 下载审计日志文件 | | |

---

## 3. 性能测试

### 3.1 测试工具

- **HTTP 压测**：`wrk` 或 `ab`（Apache Bench）
- **前端性能**：Chrome DevTools Lighthouse / Performance 面板
- **资源监控**：`docker stats`

### 3.2 页面加载时间

| 页面 | 首次加载 (FCP) | 完全加载 (Load) | 预期标准 | 是否达标 |
|------|---------------|----------------|---------|---------|
| Dashboard | | | < 2s | |
| Incidents | | | < 2s | |
| Services | | | < 2s | |
| Alert Rules | | | < 2s | |
| Metrics | | | < 2s | |
| Topology | | | < 3s | |
| AI Insights | | | < 2s | |
| Integrations | | | < 2s | |
| Team | | | < 2s | |
| Login | | | < 1s | |

### 3.3 API 响应时间

```bash
# 使用 ab 进行压测
ab -n 1000 -c 10 -H "Authorization: Bearer <token>" \
  http://<服务器IP>/api/v1/dashboard/summary
```

| API 端点 | 请求数 | 并发 | 平均响应 | p95 响应 | p99 响应 | 预期 p95 | 是否达标 |
|---------|--------|------|---------|---------|---------|---------|---------|
| GET /healthz | 1000 | 10 | | | | < 50ms | |
| GET /api/v1/dashboard/summary | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/incidents | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/services | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/alert-rules | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/metrics/query | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/topology | 1000 | 10 | | | | < 200ms | |
| GET /api/v1/insights | 1000 | 10 | | | | < 200ms | |
| POST /api/v1/auth/login | 200 | 5 | | | | < 200ms | |

### 3.4 并发用户测试

```bash
# 模拟 50 并发用户
ab -n 5000 -c 50 http://<服务器IP>/api/v1/dashboard/summary
```

| 测试指标 | 测量值 | 预期标准 | 是否达标 |
|---------|--------|---------|---------|
| 并发用户数 | 50 | ≥ 50 | |
| 总请求数 | 5000 | - | |
| 失败请求数 | | 0 | |
| 平均响应时间 (ms) | | < 500ms | |
| p95 响应时间 (ms) | | < 1000ms | |
| 吞吐量 (req/s) | | > 100 | |
| CPU 使用率 | | < 80% | |
| 内存使用率 | | < 80% | |
| 数据库连接数 | | < 100 | |

### 3.5 长时间稳定性测试

```bash
# 持续运行 10 分钟
wrk -t4 -c20 -d600s http://<服务器IP>/api/v1/services
```

| 测试项 | 持续时间 | 结果 | 结论 |
|--------|---------|------|------|
| 无内存泄漏 | 10 分钟 | | |
| 响应时间稳定 | 10 分钟 | | |
| 无连接泄漏 | 10 分钟 | | |
| 日志无异常增长 | 10 分钟 | | |

---

## 4. 安全测试

### 4.1 登录安全

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SEC-001 | 暴力破解防护 | 连续 5 次输入错误密码 | 账户被锁定 15 分钟，返回 429 | | |
| SEC-002 | 密码加密传输 | 抓包查看登录请求 | 密码通过 HTTPS 加密传输 | | |
| SEC-003 | Token 随机性 | 多次登录获取 Token | 每次生成的 Token 不同 | | |
| SEC-004 | JWT 签名验证 | 修改 Token 内容后访问 API | 返回 401 未授权 | | |

### 4.2 访问控制

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SEC-005 | 未授权 API 访问 | 不带 Token 访问 /api/v1/services | 返回 401 未授权 | | |
| SEC-006 | 伪造 Token | 使用无效 Token 访问 API | 返回 401 未授权 | | |
| SEC-007 | 越权访问 | 使用 Viewer 角色 Token 执行管理操作 | 返回 403 禁止访问 | | |
| SEC-008 | API 路径遍历 | 尝试访问 ../../etc/passwd 等路径 | Nginx 返回 403/404 | | |

### 4.3 速率限制

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SEC-009 | API 频率限制 | 1 秒内发送超过 100 个请求 | 超出限制后返回 429 Too Many Requests | | |
| SEC-010 | 限制恢复 | 等待限制窗口后重试 | 恢复正常访问 | | |
| SEC-011 | 健康检查端点不限流 | 高频访问 /healthz | 不触发限流 | | |

### 4.4 Web 安全防护

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SEC-012 | XSS 防护 | 在输入框中输入 `<script>alert(1)</script>` | 内容被转义，不执行脚本 | | |
| SEC-013 | SQL 注入防护 | 在查询参数中注入 SQL：`' OR '1'='1` | 返回正常错误，不泄露数据 | | |
| SEC-014 | X-Content-Type-Options | 检查响应头 | 包含 `X-Content-Type-Options: nosniff` | | |
| SEC-015 | X-Frame-Options | 检查响应头 | 包含 `X-Frame-Options: SAMEORIGIN` | | |
| SEC-016 | 隐藏文件访问 | 访问 `/.env` 或 `/.git/config` | 返回 403 禁止访问 | | |

### 4.5 数据安全

| 用例ID | 测试项 | 操作步骤 | 预期结果 | 实际结果 | 通过/失败 |
|--------|--------|---------|---------|---------|----------|
| SEC-017 | 敏感信息不泄露 | 触发 API 错误 | 错误响应不包含堆栈信息或数据库细节 | | |
| SEC-018 | 密码不明文存储 | 查看数据库用户表 | 密码字段为哈希值 | | |
| SEC-019 | 审计日志完整性 | 执行各种操作后查看审计记录 | 所有操作均有记录，不可删除 | | |

---

## 5. 兼容性测试

### 5.1 浏览器兼容性

| 浏览器 | 版本 | 页面渲染 | 功能完整 | 性能正常 | 通过/失败 |
|--------|------|---------|---------|---------|----------|
| Chrome | 最新版 | | | | |
| Firefox | 最新版 | | | | |
| Edge | 最新版 | | | | |
| 360 安全浏览器 | 最新版 | | | | |
| 奇安信浏览器 | 最新版 | | | | |

### 5.2 分辨率适配

| 分辨率 | 布局正常 | 表格可操作 | 图表可读 | 通过/失败 |
|--------|---------|-----------|---------|----------|
| 1920×1080 | | | | |
| 1680×1050 | | | | |
| 1440×900 | | | | |
| 1366×768 | | | | |

---

## 6. 验收结论

### 6.1 测试统计

| 统计项 | 数量 |
|--------|------|
| 测试用例总数 | |
| 通过 | |
| 失败 | |
| 阻塞 | |
| 通过率 | % |

### 6.2 遗留问题

| 编号 | 问题描述 | 严重程度 | 解决方案 | 预计修复时间 |
|------|---------|---------|---------|------------|
| | | | | |

### 6.3 验收意见

| 角色 | 意见 | 签字 | 日期 |
|------|------|------|------|
| 测试负责人 | | | |
| 项目经理 | | | |
| 客户方代表 | | | |

---

> **测试结果说明**：
> - 所有功能测试用例必须 100% 通过
> - 所有性能指标必须达标
> - 所有安全测试用例必须通过
> - 存在未解决的阻塞级问题不允许通过验收
